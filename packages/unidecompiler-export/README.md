# unidecompiler-export

Host-side, format-neutral file writers for already recovered unidecompiler
results. This package is intentionally separate from the core engine: it does
not participate in decoding, lifting, CFG recovery, simulation, or rendering.
